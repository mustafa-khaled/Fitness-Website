import styles from "./errorPage.module.css";

const First = () => {
  return (
    <div className={`${styles.first} margin-sections main-background`}>
      <h1 className="main-heading">Error Page</h1>
    </div>
  );
};

export default First;
