import styles from "./home.module.css";

const SecondSection = () => {
  return (
    <div className={`${styles.second_section} container`}>
      <h1>hi</h1>
    </div>
  );
};

export default SecondSection;
