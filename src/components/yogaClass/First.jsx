import styles from "./yogaClass.module.css";

const First = () => {
  return (
    <div className={`${styles.first} margin-sections `}>
      <h1>Yoga As Therapy</h1>
    </div>
  );
};

export default First;
